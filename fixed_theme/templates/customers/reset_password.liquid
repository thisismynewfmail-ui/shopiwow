<section class="section">
  <div class="container" style="max-width: 460px;">
    <header class="section-header">
      <h1>{{ 'customer.reset_password.title' | t }}</h1>
      <p class="text-muted">{{ 'customer.reset_password.subtext' | t: email: email }}</p>
    </header>

    {%- form 'reset_customer_password' -%}
      {%- if form.errors -%}
        <div class="card mb-md" style="border-color: var(--color-error); color: var(--color-error);">
          {{ form.errors | default_errors }}
        </div>
      {%- endif -%}
      <div class="field">
        <label for="NewPassword">{{ 'customer.reset_password.password' | t }}</label>
        <input id="NewPassword" type="password" name="customer[password]" required>
      </div>
      <div class="field">
        <label for="ConfirmPassword">{{ 'customer.reset_password.password_confirm' | t }}</label>
        <input id="ConfirmPassword" type="password" name="customer[password_confirmation]" required>
      </div>
      <button type="submit" class="btn btn--primary btn--block btn--lg">{{ 'customer.reset_password.submit' | t }}</button>
    {%- endform -%}
  </div>
</section>
